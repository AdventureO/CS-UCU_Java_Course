package com.pryhoda.factory;

import com.pryhoda.flowers.FlowerBucket;

/**
 * Created by pryho on 22-Nov-16.
 */
public class WeddingBacket extends FlowerBucket {
/*    WeddingBacket(BucketSize sz);
    if(sz = BucketSize.L){
        for(int i = 0; i < 10; i++){
        FlowerSpec spec =  new FlowerSpec();
        spec.setType(FLowerType.CHAMOMILE);
        Flower fl = new Flower(spec);
        this.addFlower(fl);
    }*/
}
